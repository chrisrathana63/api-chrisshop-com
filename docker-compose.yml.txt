version: "3.4"

services:
  flaskdemo:
    image: flaskdemo
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "5000:5000" 
  redis:
    image: redis:latest
  db:
    image: mongo:latest